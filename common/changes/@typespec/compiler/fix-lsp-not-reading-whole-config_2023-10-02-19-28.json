{
  "changes": [
    {
      "packageName": "@typespec/compiler",
      "comment": "Fix: Language Server wasn't loading the `tspconfig.yaml` correcty resulting in some options being dropped like the linter configuration.",
      "type": "none"
    }
  ],
  "packageName": "@typespec/compiler"
}