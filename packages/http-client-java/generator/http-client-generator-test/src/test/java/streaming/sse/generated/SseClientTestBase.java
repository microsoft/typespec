// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) TypeSpec Code Generator.

package streaming.sse.generated;

// The Java test files under 'generated' package are generated for your reference.
// If you wish to modify these files, please copy them out of the 'generated' package, and modify there.
// See https://aka.ms/azsdk/dpg/java/tests for guide on adding a test.

import com.azure.core.http.policy.HttpLogDetailLevel;
import com.azure.core.http.policy.HttpLogOptions;
import com.azure.core.test.TestMode;
import com.azure.core.test.TestProxyTestBase;
import com.azure.core.util.Configuration;
import streaming.sse.NamedClient;
import streaming.sse.RetrieveClient;
import streaming.sse.SseClientBuilder;
import streaming.sse.UnnamedClient;

class SseClientTestBase extends TestProxyTestBase {
    protected UnnamedClient unnamedClient;

    protected NamedClient namedClient;

    protected RetrieveClient retrieveClient;

    @Override
    protected void beforeTest() {
        SseClientBuilder unnamedClientbuilder = new SseClientBuilder()
            .endpoint(Configuration.getGlobalConfiguration().get("ENDPOINT", "http://localhost:3000"))
            .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
            .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.RECORD) {
            unnamedClientbuilder.addPolicy(interceptorManager.getRecordPolicy());
        }
        unnamedClient = unnamedClientbuilder.buildUnnamedClient();

        SseClientBuilder namedClientbuilder = new SseClientBuilder()
            .endpoint(Configuration.getGlobalConfiguration().get("ENDPOINT", "http://localhost:3000"))
            .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
            .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.RECORD) {
            namedClientbuilder.addPolicy(interceptorManager.getRecordPolicy());
        }
        namedClient = namedClientbuilder.buildNamedClient();

        SseClientBuilder retrieveClientbuilder = new SseClientBuilder()
            .endpoint(Configuration.getGlobalConfiguration().get("ENDPOINT", "http://localhost:3000"))
            .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
            .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.RECORD) {
            retrieveClientbuilder.addPolicy(interceptorManager.getRecordPolicy());
        }
        retrieveClient = retrieveClientbuilder.buildRetrieveClient();

    }
}
