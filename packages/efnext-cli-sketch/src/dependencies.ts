import { createPackage } from "@alloy-js/typescript";

export const CLITable3 = createPackage({
  name: "cli-table3",
  version: "0.6.5",
  descriptor: {
    ".": {
      default: "Table",
    },
  },
});
