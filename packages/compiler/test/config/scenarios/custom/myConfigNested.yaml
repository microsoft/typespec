# This has comments
emit:
  - openapi
parameters:
  service-name:
    default: "Sphere"
  owner:
    default: "Microsoft"
options:
  description:
    name: "Testing name: {service-name}"
    details:
      one: "Type: {owner}"
      two:
        two-one: "Default: {owner}"
