{
  "_comment": "Same as verifiers.json but with the 'hierarchy' category promoted to a deterministic routine. Run the demo with --verifiers verifiers.promoted.json to see the hierarchy check flip from AI to deterministic (PASS) without touching the spec assertion.",
  "access": {
    "check": "export_visibility",
    "public_init": "models/__init__.py"
  },
  "exactName": {
    "check": "identifier_exact"
  },
  "naming": {
    "check": "identifier_idiomatic_casing",
    "casing": {
      "type": "pascal",
      "model": "pascal",
      "enum": "pascal",
      "property": "snake",
      "parameter": "snake",
      "operation": "snake",
      "enumvalue": "upper_snake"
    }
  },
  "client-location": {
    "check": "operation_client_membership"
  },
  "hierarchy": {
    "check": "class_base_subtype"
  }
}
