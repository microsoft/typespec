<?xml version="1.0" encoding="utf-8"?>
<doc>
  <members>
    <member name="GetPaginationParamsAsync(int?,int?,int?,RequestContext)">
      <example>
This sample shows how to call GetPaginationParamsAsync and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.GetPaginationParamsAsync(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call GetPaginationParamsAsync with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.GetPaginationParamsAsync(1234, 1234, 1234, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
    <member name="GetPaginationParams(int?,int?,int?,RequestContext)">
      <example>
This sample shows how to call GetPaginationParams and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.GetPaginationParams(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call GetPaginationParams with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.GetPaginationParams(1234, 1234, 1234, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
    <member name="Get2sAsync(int?,int?,long?,RequestContext)">
      <example>
This sample shows how to call Get2sAsync and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.Get2sAsync(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call Get2sAsync with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.Get2sAsync(1234, 1234, 1234L, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
    <member name="Get2s(int?,int?,long?,RequestContext)">
      <example>
This sample shows how to call Get2s and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.Get2s(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call Get2s with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.Get2s(1234, 1234, 1234L, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
    <member name="Get3sAsync(int?,int?,int?,RequestContext)">
      <example>
This sample shows how to call Get3sAsync and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.Get3sAsync(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call Get3sAsync with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.Get3sAsync(1234, 1234, 1234, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
    <member name="Get3s(int?,int?,int?,RequestContext)">
      <example>
This sample shows how to call Get3s and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.Get3s(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call Get3s with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.Get3s(1234, 1234, 1234, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
    <member name="Get4sAsync(int?,int?,float?,RequestContext)">
      <example>
This sample shows how to call Get4sAsync and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.Get4sAsync(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call Get4sAsync with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

await foreach (BinaryData item in client.Get4sAsync(1234, 1234, 123.45F, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
    <member name="Get4s(int?,int?,float?,RequestContext)">
      <example>
This sample shows how to call Get4s and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.Get4s(null, null, null, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.ToString());
}
]]></code>
This sample shows how to call Get4s with all parameters and parse the result.
<code><![CDATA[
TokenCredential credential = new DefaultAzureCredential();
PaginationParamsClient client = new PaginationParamsClient(credential);

foreach (BinaryData item in client.Get4s(1234, 1234, 123.45F, null))
{
    JsonElement result = JsonDocument.Parse(item.ToStream()).RootElement;
    Console.WriteLine(result.GetProperty("id").ToString());
    Console.WriteLine(result.GetProperty("name").ToString());
    Console.WriteLine(result.GetProperty("type").ToString());
}
]]></code></example>
    </member>
  </members>
</doc>