import "@cadl-lang/rest";

import "./decorators.js";
import "./common.cadl";

using Cadl.Http;

@addProperties
model SomeModel {
  name: string;
}

@route("/ops")
namespace Ops {
  @addResponseTypes
  @addCommonParameters
  @get
  op SomeOp(input: SomeModel): OkResponse<int32>;
}
