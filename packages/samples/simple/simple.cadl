import "@cadl-lang/rest";

using Cadl.Http;

@route("/alpha/{id}")
op doAlpha(@path id: string): string;

@route("/beta/{id}")
op doBeta(@path id: string): string;

interface Some {
  @post
  @route("/other")
  other(@query id: string): string;
}
