{
  "openapi": "3.0.0",
  "info": {
    "title": "ServerPathMultiple",
    "version": "v1.0"
  },
  "tags": [],
  "paths": {
    "/": {
      "get": {
        "operationId": "noOperationParams",
        "parameters": [],
        "responses": {
          "204": {
            "description": "There is no content to send for this request, but the headers may be useful."
          }
        }
      }
    },
    "/{keyword}": {
      "get": {
        "operationId": "withOperationPathParam",
        "parameters": [
          {
            "name": "keyword",
            "in": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "There is no content to send for this request, but the headers may be useful."
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Server.Path.Multiple.Versions": {
        "type": "string",
        "enum": ["v1.0"],
        "description": "Service versions"
      },
      "Versions": {
        "type": "string",
        "enum": ["v1.0"],
        "description": "Service versions"
      }
    }
  },
  "servers": [
    {
      "url": "{endpoint}/server/path/multiple/{apiVersion}",
      "description": "Test server with path parameters.",
      "variables": {
        "endpoint": {
          "default": "",
          "description": "Pass in http://localhost:3000 for endpoint."
        },
        "apiVersion": {
          "default": "",
          "description": "Pass in v1.0 for API version.",
          "enum": ["v1.0"]
        }
      }
    }
  ]
}
