openapi: 3.0.0
info:
  title: (title)
  version: 0.0.0
tags:
  - name: consents
paths:
  /:
    get:
      operationId: getConsent
      parameters: []
      responses:
        "200":
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Consent"
      tags:
        - consents
components:
  schemas:
    Consent:
      type: object
      required:
        - id
      properties:
        id:
          type: string
