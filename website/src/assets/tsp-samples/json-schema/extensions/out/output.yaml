$schema: https://json-schema.org/draft/2020-12/schema
$id: output.yaml
$defs:
  Server:
    $schema: https://json-schema.org/draft/2020-12/schema
    $id: Server
    type: object
    properties:
      hostname:
        type: string
        format: hostname
    required:
      - hostname
    x-tag:
      validate: true
      category: users
