---
changeKind: feature
packages:
  - "@typespec/http-client-python"
---

Expose SSE event IDs and retry values on structured streams, support text payloads for event envelopes, and automatically reconnect streams when the server provides a retry interval.
