---
changeKind: feature
packages:
  - "@typespec/http-client-csharp"
---

Add `ArgumentExpression` class that wraps a `ValueExpression` with optional `ref`/`out` modifiers, cleanly separating argument-passing semantics from variable reference semantics.
