---
changeKind: internal
packages:
  - "@typespec/http-client-python"
---

Expand sync and async mock_api tests for the `encode/duration` Spector scenarios (milliseconds, larger-unit, and array encodings for query, property, and header) for both azure and unbranded flavors.
