---
changeKind: feature
packages:
  - "@typespec/http-client-java"
---

Add the `max-overload: model` option to generate maximum `WithResponse` overloads with strongly typed request and response models.
