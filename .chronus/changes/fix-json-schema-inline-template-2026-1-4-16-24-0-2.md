---
# Change versionKind to one of: internal, fix, dependencies, feature, deprecation, breaking
changeKind: fix
packages:
  - "@typespec/asset-emitter"
---

Keep lexical context for when working with template instance without name
