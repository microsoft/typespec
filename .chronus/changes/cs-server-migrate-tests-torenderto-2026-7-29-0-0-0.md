---
changeKind: internal
packages:
  - "@typespec/http-server-csharp"
---

Migrate `controller-action`, `enums`, and `type-expression` tests from substring-matching assertions to focused `.toRenderTo` component tests in their existing test files.
