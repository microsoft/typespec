---
changeKind: fix
packages:
  - "@typespec/http-server-csharp"
---

Add missing default values (`false` for `skip-format`, `null` for `http-port` and `https-port`) to emitter options schema and documentation.
