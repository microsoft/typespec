---
changeKind: feature
packages:
  - "@typespec/spec-coverage-sdk"
---

Add `CompilerBenchmarkClient` and related types to store and retrieve TypeSpec compiler performance benchmark results keyed by commit SHA.
