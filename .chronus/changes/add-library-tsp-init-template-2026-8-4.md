---
changeKind: feature
packages:
  - "@typespec/compiler"
---

Add new `library-tsp` `tsp init` template to create a shared spec library containing only TypeSpec types (no TypeScript)

```bash
tsp init --template library-tsp
```
